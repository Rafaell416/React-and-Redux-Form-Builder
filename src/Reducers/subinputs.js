
const subinputs = (state=[], action) => {
    switch (action.type){
        case 'ADD_SUB_INPUT':
            return state
        default:
            return state
    }
}

export default subinputs
